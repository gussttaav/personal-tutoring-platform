---
slug: TODO-slug-igual-que-el-nombre-del-fichero
title: "TODO — el título que ve el estudiante"
block: 1
order: 1
minutes: 25
summary: "TODO — una frase. Aparece en el índice del curso y en los metadatos de la página."
draft: true
hasCode: true      # `true` porque abajo hay un <PyCell>. Si lo borras, pon `false`.
hasQuiz: true
quiz:
  - id: q-TODO
    type: single
    prompt: "TODO — el enunciado. Admite **markdown** y $\\LaTeX$."
    options:
      - { id: a, text: "TODO" }
      - { id: b, text: "TODO" }
    answer: a
    explanation: "TODO — por qué. Obligatorio: una pregunta sin explicación no enseña nada."
challenges: []
---

{/*
  COURSE-P5-00 — Plantilla de lección. Cópiala a `es/NN-slug.mdx` y bórralo todo.

  El prefijo `_` mantiene este fichero fuera del registro y de `pnpm lint:content`,
  así que puede contener TODOs sin romper CI. Un fichero de trabajo se llama igual:
  `_loquesea.mdx`, o cualquier cosa dentro de `_drafts/`.

  Antes de escribir: docs/courses/AUTHORING.md (estructura, presupuesto, tono, trampas
  de MDX/LaTeX) y docs/courses/NOTATION.md (notación, para todo el curso).

  La regla que más fácil se rompe (AUTHORING.md §2): esta lección sólo puede usar los
  prerrequisitos del curso y lo enseñado en las lecciones ANTERIORES. Lo que a ti te
  resulta evidente porque ya sabes el bloque 5 es deuda que el estudiante todavía no
  puede pagar. Si necesitas un concepto posterior, o lo dices en voz alta («eso es el
  bloque 2; por ahora basta con…») o el argumento se puede hacer más abajo — y casi
  siempre se puede, y casi siempre sale mejor.

  Un concepto, una palabra, en todo el curso (AUTHORING.md §5, «Terminology»). `modelo`,
  `red neuronal` y `sistema` NO son sinónimos: el lector no puede distinguir un sinónimo
  de una distinción, y se pasa el párrafo decidiendo si el cambio significaba algo. Si
  necesitas un término que no esté en la tabla, añádelo allí ANTES de escribir.

  Las cinco marcas, una tarea cada una (AUTHORING.md §5, «The five marks»):
    **negrita** el término que se DEFINE, ahí donde se define, una vez.
    *cursiva*   un anglicismo en su primer uso de la lección, o la palabra que le da
                la vuelta a la frase. Nunca un término español.
    <W>…</W>    una cadena de la que la lección HABLA (NOTATION.md §6).
    «…»         una palabra en su sentido corriente, todavía sin precisar.
    `código`    Python, y sólo Python.
  Y la voz: `tú` para lo que hace el estudiante, `nosotros` sólo para el desarrollo que
  hacéis juntos en la página. Nunca impersonal («se debe…», «el estudiante debe…»).
  Prohibidas: «obviamente», «simplemente», «sencillamente», «basta con», «cabe destacar»,
  «como podemos ver»… `pnpm lint:content` avisa de las dos familias.

  Los seis pasos marcados abajo con comentarios son el contrato, en este orden, siempre.
  PERO NO SON TÍTULOS: los comentarios se borran al escribir. Un estudiante que lee
  «## Motivación», «## Intuición», «## Formalización» lección tras lección no está
  siguiendo una explicación, está viendo rellenarse un formulario. El andamio es para ti.

  Regla: ningún título lleva el nombre de un paso, y la mayoría de los pasos no llevan
  título. Motivación, intuición y puente son prosa corrida — el principio y el final de
  la lección. Sólo cambian de modo, y por tanto sólo se titulan, los sitios donde el
  estudiante deja de leer y hace algo. Y el título dice qué se hace AQUÍ:
  «## Construyendo la matriz one-hot en NumPy», nunca «## Implementación».
  Dos a cuatro `##` por lección. `pnpm lint:content` avisa si se te cuela un paso.
*/}

{/* ── 1. MOTIVACIÓN — sin título. Es el arranque de la lección. ───────────────────
       SI ESTA NO ES LA PRIMERA LECCIÓN: la PRIMERA FRASE RECOGE EL PUENTE DE LA
       ANTERIOR antes de plantear su propia pregunta, y la nombra por su tema, no sólo
       por su número («La lección anterior, dedicada al problema de representar el
       lenguaje, terminó con una cuestión pendiente»). La primera lección de un BLOQUE
       recoge la última del bloque anterior: eso es lo que hace que cinco bloques sean
       un curso. Un puente que nadie recoge es una promesa que el curso no cumple.

       NO HAY NÚMERO DE PALABRAS, porque el arranque lo leen dos personas distintas: quien
       viene de terminar la lección anterior y quien vuelve al curso quince días después.
       Al primero le sobra todo lo que ya sabe; al segundo hay que devolverlo al sitio. Lo
       que decide no es la longitud sino la FORMA: afirma, no vuelvas a deducir; dilo con
       otras palabras y en otro orden; y no repitas el ejemplo del puente —si ese ejemplo
       es el asunto de esta lección, va más abajo, donde haga trabajo nuevo—.

       LA COMPROBACIÓN, con los dos lectores. Lee el final de la lección anterior y este
       arranque seguidos: nada debería leerse dos veces. Después lee el arranque solo y en
       frío: quien lleva quince días sin abrir el curso tiene que acabarlo sabiendo dónde
       está. Fallar lo primero es un resumen; fallar lo segundo es una puerta sin pomo, y
       es peor. ─────────────────────────────────────────────────────────────────────── */}

TODO — dos párrafos. La pregunta que responde esta lección, y por qué le importa a alguien. No
resumas lo que viene: plantea el problema tan bien que el estudiante quiera la respuesta.

TODO — segundo párrafo, ÍNTEGRAMENTE NUEVO. Si puedes, un ejemplo concreto en español que falle de
forma visible.

{/* ── 2. INTUICIÓN — sin título. Sale de la motivación, no se anuncia. ────────── */}

TODO — la idea en palabras y dibujos, **antes de cualquier símbolo**. Aquí suele ir un explorable:
que el estudiante mueva algo y vea qué cambia antes de leer la fórmula que lo describe.

{/* Ids válidos en src/features/courses/widgets/widget-ids.ts. Un id que no exista es
    un error duro del lint. Borra la línea si esta lección no lleva widget. */}

<Explorable id="tokenizer-playground" caption="TODO — qué debe mirar el estudiante aquí." />

{/* ¿LLEVA FIGURA? Pregúntatelo aquí, que es donde toca, y no lo dejes en «no» por
    omisión (AUTHORING.md §7, «When a figure earns its place»). Dos casos la piden:

      1. El argumento es una FORMA o una POSICIÓN. Si la prosa se apoya en «entre»,
         «por debajo», «paralelas», «opuestas», «perpendicular», «la franja», le estás
         pidiendo al lector que dibuje él el cuadro a partir de una descripción, que es
         justo lo que peor hace la prosa.
      2. El objeto tiene un dibujo CONVENCIONAL que el estudiante se va a encontrar en
         cualquier paper y en cualquier otro curso —la red por capas, la recurrencia
         desplegada, la pila encoder/decoder—.

    Y casi siempre no la lleva: nada de redibujar una ecuación, repetir una tabla ni
    duplicar el explorable que ya está arriba.

    La figura SUSTITUYE a la prosa que la describiría, no la acompaña. El `caption` dice
    la conclusión; el `alt` describe el dibujo, y es para quien no puede verlo. Los dos
    son texto plano: ni markdown ni LaTeX.

    El SVG va en public/courses/dl-nlp/, escrito a mano, con los hex del tema puestos a
    pelo y el fondo transparente. Descoméntalo si la lección lleva figura, y bórralo si
    no. (Va comentado a propósito: el lint valida los ids de Explorable, PyCell, Quiz y
    CodeChallenge, pero NO la `src` de una Figure, así que una que se quede aquí olvidada
    da un 404 en silencio.)

<Figure
  src="/courses/dl-nlp/TODO.svg"
  alt="TODO — qué se ve en el dibujo, para quien no puede verlo."
  caption="TODO — qué tiene que concluir el estudiante, no lo que ya está viendo."
/>
*/}

<Callout type="intuition" title="La idea en una frase">
TODO — el resumen que te gustaría que recordara dentro de seis meses.
</Callout>

{/* ── 3. FORMALIZACIÓN — suele llevar título, porque cambia el modo y el estudiante
       querrá volver aquí. Titúlalo por lo que se formaliza, no «Formalización».
       Se subdivide POR EL ARGUMENTO, no por la longitud: un título marca una AFIRMACIÓN
       nueva, no una pantalla nueva. Dos movimientos (qué ES el objeto, y qué CUESTA) son
       dos títulos aunque sean cortos; un movimiento largo es un título aunque ocupe una
       página. En orden de dependencia: nada bajo un título puede apoyarse en algo que se
       establece bajo uno posterior. Pasados ~600 palabras bajo un mismo título, ve a
       buscar el segundo movimiento: casi siempre está ahí sin nombrar. ───────────── */}

## TODO — qué se define o se desarrolla aquí

TODO — las matemáticas, completas. El desarrollo se muestra, no se afirma: es lo que diferencia a
este curso. Di la forma de cada array la primera vez que aparece.

$$
\mathbf{h}^{(l)} = \sigma\!\left(\mathbf{W}^{(l)} \mathbf{h}^{(l-1)} + \mathbf{b}^{(l)}\right),
$$

{/* Las vallas `$$` van en su propia línea. En una sola línea, `$$…$$` se renderiza
    como matemática EN LÍNEA — y el lint la cuenta así.

    La ecuación forma parte de la frase y lleva su puntuación, como último carácter
    DENTRO de la valla: punto si la frase termina ahí, coma si lo que sigue pediría una
    pausa —como este «con …»—, nada si la frase la atraviesa. */}

con $\mathbf{W}^{(l)} \in \mathbb{R}^{d_{\text{out}} \times d_{\text{in}}}$.

<Details summary="TODO — ver el desarrollo completo">
TODO — los pasos largos van aquí. Plegados, no ausentes: el estudiante que quiere el detalle lo
tiene, y el que no, sigue leyendo.
</Details>

{/* ── 4. IMPLEMENTACIÓN — con título: «deja de leer y ejecuta esto». Di qué se
       construye. Si la lección no lleva código a propósito, es un ejemplo resuelto a
       mano («## Compruébalo a mano con…»). Lo que no puede es faltar. ────────────── */}

## TODO — qué construye el estudiante aquí

TODO — NumPy desde cero. Una celda que el estudiante ejecuta de verdad.

{/* `code` es una PROP, no children: MDX parsearía el cuerpo como markdown y
    destrozaría la indentación, que en Python es sintaxis. Si añades una celda,
    `hasCode` pasa a `true` (el lint lo comprueba en ambas direcciones). */}

<PyCell packages={["numpy"]} code={`
import numpy as np

# TODO
print(np.arange(3))
`} />

{/* ── 5. VERIFICACIÓN — con título: «deja de leer y ponte a prueba». Algo como
       «## Comprueba tu intuición», nunca «## Verificación». ─────────────────────── */}

## TODO — invita a comprobar, con las palabras de esta lección

TODO — una o dos frases que enlacen la pregunta con lo que se acaba de explicar.

<Quiz id="q-TODO" />

{/* Y, si hay algo que construir, añade un desafío: decláralo en `challenges:` arriba y
    colócalo aquí con la etiqueta CodeChallenge y su id.

    OJO: el lint lee el fichero con expresiones regulares, no con un parser de MDX, así
    que una etiqueta escrita DENTRO de un comentario cuenta igual. No comentes un
    Quiz, un PyCell ni un CodeChallenge para desactivarlo — bórralo. */}

{/* ── 6. PUENTE — SIN TÍTULO, y esto importa más que en ningún otro paso. Un `##` que
       diga «Puente» enseña el mecanismo de la peor forma posible. Es el párrafo final
       de la lección, sin anunciarse.

       Sí lleva una marca, y es la línea de abajo: `---`, una raya centrada y corta. El
       puente viene DESPUÉS de una sección (normalmente el quiz), así que sin marca se
       lee como una coletilla de la última pregunta. La raya dice «el argumento se cierra
       aquí» sin decir qué es lo que se cierra.

       DOS REGLAS. El `---` va precedido de una línea EN BLANCO: pegado al párrafo de
       arriba, markdown lo lee como un título (setext) y falla en silencio. Y es el único
       de la lección: `---` está reservado para el puente y no se usa en ningún otro
       sitio. `pnpm lint:content` comprueba las dos cosas.

       Y DOS LÍMITES. Nombra la lección SIGUIENTE, sólo esa: lo que esté más allá es una
       indicación («eso ocupa el resto del bloque»), no una promesa con nombre, porque dos
       lecciones que prometen la misma dejan a la segunda sin puerta que abrir. Y son DOS
       PÁRRAFOS, como la motivación que refleja; el tercero casi siempre es material que
       iba por encima del `---`. ──────────────────────────────────────────────────── */}

---

TODO — **lo más importante de la lección**. Qué queda sin resolver, y qué lección lo resuelve.
Esto es lo que convierte el curso en un curso y no en un montón de tutoriales sueltos. Termina
siempre mirando hacia adelante.

TODO — segundo párrafo. Aquí es donde se nombra la lección siguiente, por su tema. Esta es la puerta
que su primera frase va a recoger: escríbela sabiendo que alguien tiene que atravesarla.
