---
slug: luong
title: "Atención de Luong (multiplicativa)"
block: 4
order: 5
minutes: 27
summary: "La misma casilla de la lección anterior, llena de otra forma: en vez de proyectar los dos estados y aplastar su suma, se multiplican a través de una sola matriz. Sale una puntuación sin capa oculta, con casi los mismos pesos y un gradiente más corto, y con una propiedad que la aditiva no tiene: la rejilla entera de puntuaciones es un producto de matrices. Aquí se comparan las dos por coste —operaciones, memoria intermedia y forma del cálculo— y se dice qué impide todavía calcular la rejilla de una sola vez."
draft: false
hasCode: true
hasQuiz: true
quiz:
  - id: q-que-mide-wa
    type: single
    prompt: '¿Qué añade $\mathbf{W}_a$ frente a puntuar con el producto escalar pelado, $e_{ij} = \mathbf{s}_{i-1}^{\top}\bar{\mathbf{h}}_j$?'
    options:
      - { id: a, text: 'Una base común. El producto escalar da por hecho que la coordenada $k$ del decoder habla de lo mismo que la coordenada $k$ del encoder, y con la misma escala; $\mathbf{W}_a$ reescribe un estado en las coordenadas del otro antes de medir, y de paso permite que los dos lados ni siquiera midan lo mismo.' }
      - { id: b, text: 'Una no linealidad. Sin ella la puntuación sería lineal en cada estado y no podría distinguir parejas; con la matriz en medio deja de serlo.' }
      - { id: c, text: 'La normalización. La matriz reparte la fila de forma que las $T_x$ puntuaciones acaben sumando $1$, que es lo que el producto escalar por sí solo no garantiza.' }
      - { id: d, text: 'La dependencia de la pareja. El producto escalar opina por separado sobre cada vector, mientras que la matriz mezcla los dos, que es la condición que la lección anterior declaró irrenunciable.' }
    answer: a
    explanation: 'La (a). Los dos estados salen de dos recurrencias distintas, entrenadas para dos trabajos distintos, y nada las obliga a haber elegido las mismas direcciones para las mismas cosas; $\mathbf{W}_a$ es exactamente el cambio de base que falta, aprendido junto al resto. La (b) confunde dos propiedades: la puntuación multiplicativa **sigue** siendo lineal en cada estado por separado —es una forma bilineal—, y aun así depende de la pareja, que es lo que hacía falta. La (c) le da a la matriz el trabajo del softmax, que es quien normaliza. La (d) es la trampa buena, y se cae escribiendo el producto escalar: $\mathbf{s}_{i-1}^{\top}\bar{\mathbf{h}}_j$ no se puede partir en una opinión sobre un vector más otra sobre el otro, así que ya es función de la pareja. Lo que le falta no es eso.'
    hint: 'Si las dos redes hubieran aprendido a usar sus coordenadas de forma distinta, ¿qué mediría el producto escalar?'
  - id: q-parametros-luong
    type: numeric
    prompt: 'Un encoder y un decoder de $d_h = 64$ coordenadas. ¿Cuántos pesos tiene la puntuación multiplicativa?'
    answer: 4096
    tolerance: 0
    unit: pesos
    explanation: 'Una sola matriz de $64 \times 64$: $4\,096$ pesos. Compárala con la aditiva del mismo tamaño, que con $d_a = 32$ pedía $2 \cdot 32 \cdot 64 + 32 = 4\,128$: treinta y dos pesos de diferencia sobre cuatro mil. La elección entre las dos no se decide ahí, y conviene tenerlo claro antes de mirar los costes —lo que separa a las dos puntuaciones es qué operaciones hacen con sus pesos, no cuántos tienen—. Nótese además que ninguna de las dos cuentas menciona $T_x$ ni $T_y$: el juego de pesos es uno para todas las parejas de todas las frases.'
    hint: '¿De qué forma es la matriz, y hay algo más que contar?'
  - id: q-memoria-intermedia
    type: single
    prompt: 'Puntúas una pareja de frases de $T_x = T_y = 60$ tokens con $d_h = d_a = 256$. ¿Qué guarda la puntuación aditiva que la multiplicativa no guarda?'
    options:
      - { id: a, text: 'La rejilla de vectores anterior a la lectura: $60 \cdot 60$ vectores de $256$ coordenadas, $921\,600$ números, porque el $\tanh$ se aplica a cada uno antes de que $\mathbf{v}_a$ lo reduzca a una puntuación. La multiplicativa pasa de los estados a las $3\,600$ puntuaciones sin construir nada más grande que la propia rejilla.' }
      - { id: b, text: 'Las dos proyecciones de los estados, $60 + 60$ vectores de $256$ coordenadas, que la multiplicativa se ahorra porque no proyecta nada.' }
      - { id: c, text: 'Las $3\,600$ puntuaciones sin normalizar. La multiplicativa no las guarda: el softmax se aplica dentro del mismo producto de matrices.' }
      - { id: d, text: 'Nada. Las dos guardan los mismos números intermedios; lo que cambia es cuántas multiplicaciones hacen con ellos.' }
    answer: a
    explanation: 'La (a), y el factor es exactamente $d_a$: un número por pareja frente a $d_a$ números por pareja. La causa es la de siempre —una función no lineal aplicada a una suma no se reparte entre sus sumandos—, así que el bloque de $(T_y, T_x, d_a)$ hay que construirlo entero antes de leerlo. En coma flotante de doble precisión son $7.4$ MB para una sola pareja de frases, y multiplícalo por el tamaño del *batch*. La (b) invierte los papeles: la multiplicativa también proyecta un lado, y esas proyecciones son la parte barata en las dos. La (c) se inventa un softmax que no existe: normalizar es una operación aparte, y las puntuaciones sin normalizar se guardan igual en las dos. La (d) es justo lo que el recuento desmiente.'
    hint: '¿En qué momento del cálculo aditivo hay un número por cada pareja, y en qué momento hay $d_a$?'
  - id: q-sigue-secuencial
    type: single
    prompt: 'Con la puntuación multiplicativa, ¿puede un decoder recurrente calcular la rejilla entera $\mathbf{S}\mathbf{W}_a\bar{\mathbf{H}}^{\top}$ de una sola vez mientras traduce?'
    options:
      - { id: a, text: 'No, porque $\mathbf{S}$ todavía no existe: la fila $i$ pide $\mathbf{s}_{i-1}$, que sale de una recurrencia que necesita $\mathbf{c}_{i-1}$, que sale de la fila anterior. Lo único que se saca fuera del bucle es $\mathbf{W}_a\bar{\mathbf{H}}^{\top}$, que sólo depende de la fuente.' }
      - { id: b, text: 'Sí. Un producto de matrices no impone ningún orden entre sus filas, así que las $T_y$ se calculan a la vez.' }
      - { id: c, text: 'No, porque el softmax va por filas y hay que recorrerlas en orden para que cada una sume $1$.' }
      - { id: d, text: 'Sí durante el entrenamiento: el *teacher forcing* da de antemano los $T_y$ tokens correctos, así que los estados del decoder están todos disponibles antes de empezar.' }
    answer: a
    explanation: 'La (a). El producto está escrito, y lo que falta no es la operación sino uno de sus operandos. La (d) es la respuesta que hay que descartar con cuidado, porque acierta en lo que da el *teacher forcing* y falla en lo que hace falta: tener los **tokens** de antemano no da los **estados**, que siguen saliendo de la recurrencia uno detrás de otro, cada uno con el contexto que su propio paso mezcló. La (b) confunde una propiedad del álgebra con una propiedad del cálculo: la matriz $\mathbf{S}$ no está prohibida, está por construir. La (c) atribuye un orden al softmax que no tiene —las filas se normalizan de forma independiente—. Guarda la conclusión: la puntuación ha dejado de ser el cuello, y lo que queda estrangulando el paralelismo es la recurrencia.'
    hint: '¿Qué hace falta para escribir la fila $i$ de $\mathbf{S}$, y quién lo produce?'
challenges:
  - id: ch-atencion-multiplicativa
    prompt: |
      Escribe `alineacion_multiplicativa(S, H, Wa)`. Recibe los estados del decoder `S`, de forma
      $(T_y, d_s)$ —la fila $i$ es $\mathbf{s}_{i-1}$—, los del encoder `H`, de forma $(T_x, d_h)$,
      y la matriz de la puntuación, de forma $(d_s, d_h)$. Devuelve la matriz $\mathbf{A}$ de
      forma $(T_y, T_x)$, ya normalizada por filas.

      Es la puntuación de la lección, $e_{ij} = \mathbf{s}_{i-1}^{\top}\mathbf{W}_a\bar{\mathbf{h}}_j$,
      seguida de un softmax sobre las posiciones de la entrada. Escríbela con productos de
      matrices, sin recorrer las parejas una a una, y no escribas en los arrays que recibes.
    packages: [numpy]
    starter: |
      import numpy as np


      def alineacion_multiplicativa(S, H, Wa):
          # tu código aquí
          pass
    tests:
      - name: "la forma es (T_y, T_x) y cada fila suma 1"
        code: |
          import numpy as np
          rng = np.random.default_rng(5)
          S, H = rng.normal(size=(4, 6)), rng.normal(size=(9, 6))
          Wa = rng.normal(size=(6, 6))
          A = alineacion_multiplicativa(S, H, Wa)
          assert A is not None, 'alineacion_multiplicativa tiene que devolver la matriz, no imprimirla'
          A = np.asarray(A)
          assert A.shape == (4, 9), 'una fila por paso de la salida y una columna por posicion de la entrada'
          assert np.allclose(A.sum(axis=1), 1.0), 'el softmax va por filas: cada paso reparte una unidad entre las posiciones'
      - name: "coincide con puntuar las parejas una a una"
        code: |
          import numpy as np
          rng = np.random.default_rng(5)
          S, H = rng.normal(size=(4, 6)), rng.normal(size=(9, 6))
          Wa = rng.normal(size=(6, 6))
          E = np.array([[S[i] @ Wa @ H[j] for j in range(9)] for i in range(4)])
          Z = np.exp(E - E.max(axis=1, keepdims=True))
          A = alineacion_multiplicativa(S, H, Wa)
          assert A is not None, 'alineacion_multiplicativa tiene que devolver la matriz de pesos'
          assert np.allclose(np.asarray(A), Z / Z.sum(axis=1, keepdims=True)), 'la puntuacion de la pareja (i, j) multiplica S[i] contra H[j] a traves de Wa'
      - name: "los dos lados pueden medir distinto"
        code: |
          import numpy as np
          rng = np.random.default_rng(11)
          S, H = rng.normal(size=(5, 3)), rng.normal(size=(7, 8))
          Wa = rng.normal(size=(3, 8))
          A = alineacion_multiplicativa(S, H, Wa)
          assert A is not None, 'alineacion_multiplicativa tiene que devolver la matriz de pesos'
          A = np.asarray(A)
          assert A.shape == (5, 7), 'Wa es (d_s, d_h): el decoder entra por la izquierda y el encoder por la derecha'
          assert np.allclose(A.sum(axis=1), 1.0), 'cada fila sigue sumando 1 aunque los dos lados midan distinto'
      - name: "con Wa = I sale el producto escalar"
        code: |
          import numpy as np
          rng = np.random.default_rng(2)
          S, H = rng.normal(size=(6, 5)), rng.normal(size=(4, 5))
          E = S @ H.T
          Z = np.exp(E - E.max(axis=1, keepdims=True))
          A = alineacion_multiplicativa(S, H, np.eye(5))
          assert A is not None, 'alineacion_multiplicativa tiene que devolver la matriz de pesos'
          assert np.allclose(np.asarray(A), Z / Z.sum(axis=1, keepdims=True)), 'sin cambio de base la puntuacion es el producto escalar de los dos estados'
      - name: "no desborda con puntuaciones grandes"
        code: |
          import numpy as np
          rng = np.random.default_rng(5)
          S, H = rng.normal(size=(4, 6)), rng.normal(size=(9, 6))
          Wa = rng.normal(size=(6, 6)) * 300
          A = alineacion_multiplicativa(S, H, Wa)
          assert A is not None, 'alineacion_multiplicativa tiene que devolver la matriz de pesos'
          assert np.isfinite(np.asarray(A)).all(), 'desborda: resta el maximo de cada fila antes de exponenciar'
      - name: "no escribe en lo que recibe"
        code: |
          import numpy as np
          rng = np.random.default_rng(14)
          S, H = rng.normal(size=(4, 6)), rng.normal(size=(9, 6))
          Wa = rng.normal(size=(6, 6))
          copias = [S.copy(), H.copy(), Wa.copy()]
          alineacion_multiplicativa(S, H, Wa)
          assert all(np.array_equal(u, v) for u, v in zip([S, H, Wa], copias)), 'no escribas en los arrays que recibes: E -= E.max(...) sobre una vista los estropea'
    solution: |
      import numpy as np


      def alineacion_multiplicativa(S, H, Wa):
          E = (S @ Wa) @ H.T                      # (T_y, T_x): la rejilla entera, dos productos
          Z = np.exp(E - E.max(axis=1, keepdims=True))
          return Z / Z.sum(axis=1, keepdims=True)
    explanation: |
      Toda la puntuación son dos productos de matrices y ningún bucle: `S @ Wa` reescribe los
      $T_y$ estados del decoder en las coordenadas del encoder —una fila cada uno— y el segundo
      producto los contrae contra los $T_x$ estados de la fuente, dejando un número por pareja.
      El paréntesis no cambia el resultado pero sí el trabajo: `Wa @ H.T` sólo depende de la
      fuente, así que un traductor de verdad lo calcularía una vez por frase y lo reaprovecharía
      en los $T_y$ pasos. La resta del máximo es la invariancia al desplazamiento de <Leccion slug="funcion-de-perdida">la lección
      sobre funciones de pérdida</Leccion>, usada como allí: cambia lo que se exponencia, no
      lo que sale.
---

{/* COURSE-P5-04 — Bloque 4, lección 5. */}

Un modelo con atención puntúa parejas: un número por cada paso de la salida y cada posición de la
entrada, y de esos números salen los pesos con los que se mezclan los estados del encoder. La
lección anterior, sobre la atención aditiva de Bahdanau, llenó esa casilla con una red pequeña —una
capa oculta de $d_a$ unidades y una lectura lineal, entrenada junto al resto— y dejó una factura
apuntada: puntuar una pareja obliga a fabricar un vector entero de $d_a$ coordenadas antes de
reducirlo al número que hacía falta.

La pregunta de esta lección es si hace falta una capa oculta para decir que dos vectores se
corresponden. El álgebra lineal lleva desde el bloque 1 devolviendo ese número sin ninguna red de por
medio, y en 2015 Luong, Pham y Manning propusieron usarlo tal cual, con un solo retoque. Lo que sale
no se parece a una red: se parece a un producto. Y como la calidad de las dos formas es comparable
—eso lo miden los experimentos de sus artículos, no una derivación—, lo que decide entre ellas es
todo lo demás: cuánto cuestan, qué memoria mueven y qué forma tiene su cálculo.

Vuelve a lo que la puntuación tiene que hacer. Un número que sube cuando dos vectores apuntan al
mismo sitio ya está en el curso desde <Leccion slug="bolsa-de-palabras">la lección sobre la bolsa de palabras</Leccion>: el
producto escalar $\mathbf{u}^{\top}\mathbf{v}$, que la similitud coseno se limita a
normalizar. Sería una puntuación gratis —cero parámetros— y sería función de la pareja, que es la
condición que la lección anterior declaró irrenunciable: $\mathbf{s}_{i-1}^{\top}\bar{\mathbf{h}}_j$
no se puede partir en una opinión sobre un estado más otra sobre el otro.

Lo que falla es una suposición escondida. El producto escalar suma coordenada contra coordenada, de
modo que da por hecho que la coordenada $k$ del decoder habla de lo mismo que la coordenada $k$ del
encoder, y con la misma escala. Son dos recurrencias distintas, con dos juegos de pesos y dos
trabajos —una lee español, la otra escribe inglés—, y nada las obliga a haber elegido las mismas
direcciones para las mismas cosas; ni siquiera a tener el mismo número de coordenadas. De modo que
primero se cambia de base y después se mide.

<Callout type="intuition" title="La idea en una frase">
Puntuar una pareja es medir cuánto se parecen dos vectores; lo único que hay que aprender es en qué
base compararlos.
</Callout>

## Multiplicar en vez de sumar

La puntuación multiplicativa mete una matriz entre los dos estados y no hace nada más:

$$
e_{ij} = a\left(\mathbf{s}_{i-1}, \bar{\mathbf{h}}_j\right)
= \mathbf{s}_{i-1}^{\top}\mathbf{W}_a\bar{\mathbf{h}}_j \in \mathbb{R},
$$

con $\mathbf{W}_a \in \mathbb{R}^{d_h \times d_h}$. Se lee de derecha a izquierda:
$\mathbf{W}_a\bar{\mathbf{h}}_j$ es el estado del encoder reescrito en las coordenadas del decoder, y
el producto escalar con $\mathbf{s}_{i-1}$ mide lo que mide siempre un producto escalar. No hay capa
oculta, no hay saturación y no hay ningún espacio propio de la función: el $d_a$ de la lección
anterior desaparece del problema.

Cuenta los pesos: $d_h^2$, y ninguno depende de la pareja ni de las longitudes, igual que antes. Con
$d_h = 64$ son $4\,096$, contra los $4\,128$ que pedía la aditiva con $d_a = 32$. Prácticamente los
mismos, lo cual es útil saberlo pronto: **lo que separa a las dos puntuaciones no es cuántos pesos
tienen, sino qué operaciones hacen con ellos.**

Aquí conviene decir qué se está dejando fuera. Luong y sus coautores cambian además dónde entra el
contexto —puntúan con el estado del paso en curso, $\mathbf{s}_i$, y mezclan el contexto *después* de
la recurrencia en vez de dentro de ella— y este curso no lo sigue: mantiene la colocación de
<Leccion slug="la-idea-de-atencion">la lección sobre la idea de atención</Leccion>, para que entre aquella lección, la anterior y
ésta cambie una sola cosa. Lo que se compara es la función $a$, con todo lo demás quieto.

El gradiente es más corto que el de la aditiva, y por la misma razón por la que el cálculo lo es. Las
tres derivadas de una puntuación salen de derivar un producto de tres factores:

$$
\nabla_{\mathbf{W}_a} e_{ij} = \mathbf{s}_{i-1}\bar{\mathbf{h}}_j^{\top},
\qquad
\nabla_{\bar{\mathbf{h}}_j} e_{ij} = \mathbf{W}_a^{\top}\mathbf{s}_{i-1},
\qquad
\nabla_{\mathbf{s}_{i-1}} e_{ij} = \mathbf{W}_a\bar{\mathbf{h}}_j.
$$

Ninguna lleva un factor $1 - \tanh^{2}$, porque no hay $\tanh$ que derivar. Y todo lo que hay *por
encima* de $e_{ij}$ en la cadena es de la lección anterior, sin tocar una coma: el paso por el
softmax y la derivada
$\partial\ell/\partial e_{ij} = \alpha_{ij}\left(\nabla_{\mathbf{c}_i}\ell\right)^{\top}\left(\bar{\mathbf{h}}_j - \mathbf{c}_i\right)$
nunca mencionaron qué forma tenía $a$. La atención se queda igual; lo que cambia es el modelo de
alineación.

<Details summary="Y la acumulación sobre las parejas también es un producto">

$\mathbf{W}_a$ es uno solo para las $T_x \cdot T_y$ parejas, así que su gradiente las suma todas:

$$
\nabla_{\mathbf{W}_a}\ell = \sum_{i=1}^{T_y}\sum_{j=1}^{T_x} \frac{\partial \ell}{\partial e_{ij}}\,\mathbf{s}_{i-1}\bar{\mathbf{h}}_j^{\top},
$$

que es la rejilla de derivadas puesta entre los estados del decoder y los del encoder —dos productos
de matrices, con la rejilla en medio—. El camino de vuelta tiene la misma forma que el de ida, que es
lo que hace que la sección siguiente valga para los dos sentidos.

</Details>

### Sin matriz: el producto escalar

El caso extremo es tomar $\mathbf{W}_a$ igual a la identidad, con lo que la puntuación se queda en

$$
e_{ij} = \mathbf{s}_{i-1}^{\top}\bar{\mathbf{h}}_j,
$$

y la función $a$ se queda sin un solo parámetro que entrenar. Lo que pide a cambio es justo lo que
la matriz servía para no pedir: que los dos lados midan lo mismo —$d_h$ coordenadas en los dos— y
que además hayan aprendido a usarlas de la misma forma. Es una exigencia fuerte cuando el encoder y
el decoder son dos redes distintas, entrenadas cada una para su idioma, y por eso la matriz es la
opción que Luong deja por defecto. La variante sin parámetros no se va del curso —es la forma más
barata de puntuar que existe—, pero sólo se sostiene donde los dos lados comparten base.

## La rejilla entera en un producto de matrices

Hasta aquí las puntuaciones se han escrito de una en una. Apílalas. Guarda los estados del encoder
por filas en $\bar{\mathbf{H}} \in \mathbb{R}^{T_x \times d_h}$ —la fila $j$ es
$\bar{\mathbf{h}}_j^{\top}$, transpuesta porque en este curso los vectores son columnas— y los del
decoder en $\mathbf{S} \in \mathbb{R}^{T_y \times d_h}$, cuya fila $i$ es $\mathbf{s}_{i-1}^{\top}$,
el estado con el que se puntúa el paso $i$. Entonces las $T_x \cdot T_y$ puntuaciones son un solo
objeto:

$$
\mathbf{A} = \text{softmax}\left(\mathbf{S}\mathbf{W}_a\bar{\mathbf{H}}^{\top}\right) \in \mathbb{R}^{T_y \times T_x},
$$

con el softmax aplicado por filas, que es sobre las posiciones de la entrada, como en <Leccion slug="la-idea-de-atencion">la lección
sobre la idea de atención</Leccion>. Comprueba la casilla $(i, j)$: la fila $i$ de $\mathbf{S}\mathbf{W}_a$ es
$\mathbf{s}_{i-1}^{\top}\mathbf{W}_a$ y la columna $j$ de $\bar{\mathbf{H}}^{\top}$ es
$\bar{\mathbf{h}}_j$, así que su producto es $e_{ij}$ y no otra cosa.

La aditiva no admite esta escritura, y no por falta de maña. Sus dos proyecciones sí se sacan fuera,
pero entre la suma y la lectura está el $\tanh$, de modo que hay que materializar un bloque de tres
dimensiones —una pareja, y dentro de cada pareja $d_a$ coordenadas— antes de poder reducirlo a
números.

<Figure
  src="/courses/dl-nlp/atencion-costes.svg"
  alt="A la izquierda, un bloque tridimensional de casillas etiquetado T sub y por T sub x por d sub a, con una etiqueta que dice suma, tanh y lectura en cada casilla; una flecha lo reduce a una rejilla plana de T sub y por T sub x. A la derecha, dos rectángulos planos, uno alto de T sub y por d sub h etiquetado S por W sub a y otro ancho de d sub h por T sub x etiquetado H con barra traspuesta, unidos por un signo de producto; una flecha los lleva a una rejilla plana idéntica a la de la izquierda."
  caption="Las dos puntuaciones llegan a la misma rejilla. La aditiva construye antes un número por pareja y por coordenada del modelo de alineación; la multiplicativa va de los estados a la rejilla con dos productos y nada en medio."
/>

Los recuentos ponen precio a esa diferencia. Toma $T_x = T_y = 60$ y $d_h = d_a = 256$, que es una
frase larga y un modelo pequeño. En productos y sumas las dos andan cerca: unos $8.8$ millones la
aditiva contra unos $4.9$ millones la multiplicativa, un factor menor que dos, y en ambas la mayor
parte se la llevan las proyecciones. Donde se separan es en lo otro. La aditiva evalúa $921\,600$
veces el $\tanh$ y la multiplicativa ninguna. Y sobre todo, la aditiva tiene que guardar esos
$921\,600$ números —$7.4$ MB por cada pareja de frases, antes de multiplicar por el tamaño del
*batch*— mientras que a la multiplicativa le bastan las $3\,600$ puntuaciones y los $60$ estados ya
proyectados. Entre las dos rejillas el factor es exactamente $d_a$: un número por pareja contra
$d_a$ números por pareja.

Y hay una diferencia de forma que no aparece en ninguno de esos números. El trabajo de la
multiplicativa son dos productos de matrices; el de la aditiva son sumas y $\tanh$ sobre un bloque
tridimensional, coordenada a coordenada. La misma cantidad de aritmética repartida de las dos
maneras no cuesta lo mismo en el hardware que entrena estas redes, que está construido justamente
para lo primero —es <Leccion slug="forward-pass">la lección sobre el *forward pass*</Leccion>, medida allí con un bucle
contra un producto—.

Falta lo honesto, y es lo que abre el bloque siguiente. Escribir $\mathbf{S}$ supone tener los $T_y$
estados del decoder, y el decoder es recurrente: $\mathbf{s}_{i-1}$ necesita $\mathbf{c}_{i-1}$, que
necesita la fila $i-1$ de $\mathbf{A}$, que necesita $\mathbf{s}_{i-2}$. Mientras traduce, la rejilla
se llena fila a fila y el producto grande no llega a ocurrir; lo que sí se saca del bucle es
$\mathbf{W}_a\bar{\mathbf{H}}^{\top}$, que sólo depende de la fuente y se calcula una vez por frase,
dejando cada paso del decoder en un solo producto de matriz por vector:

$$
\left(\bar{\mathbf{H}}\mathbf{W}_a^{\top}\right)\mathbf{s}_{i-1} \in \mathbb{R}^{T_x},
$$

la fila $i$ de puntuaciones sin normalizar. La puntuación ya no es el cuello de botella del
cálculo. Lo que estrangula el paralelismo, ahora, es la recurrencia que produce los estados.

## Las dos puntuaciones, en NumPy

La primera celda calcula la alineación multiplicativa de la forma corta y comprueba que el producto
no dice nada distinto de puntuar las parejas una a una. Mira también las dos últimas líneas: con
$\mathbf{W}_a$ igual a la identidad, la puntuación se queda en el producto escalar de los dos
estados.

<PyCell packages={["numpy"]} code={`
import numpy as np

T_y, T_x, d_h, d_a = 8, 6, 5, 4
rng = np.random.default_rng(4)
S = rng.normal(size=(T_y, d_h)) * 0.6      # fila i: el estado s_{i-1} con el que se puntua el paso i
H = rng.normal(size=(T_x, d_h)) * 0.6      # fila j: el estado del encoder en la posicion j
Wa = rng.normal(size=(d_h, d_h)) * 0.4     # sin entrenar


def softmax_filas(E):
    Z = np.exp(E - E.max(axis=1, keepdims=True))     # invariancia al desplazamiento
    return Z / Z.sum(axis=1, keepdims=True)


E = (S @ Wa) @ H.T                                   # (T_y, T_x): la rejilla entera, dos productos
A = softmax_filas(E)
E_bucle = np.array([[S[i] @ Wa @ H[j] for j in range(T_x)] for i in range(T_y)])

print("formas:  S", S.shape, " H", H.shape, " Wa", Wa.shape, " ->  A", A.shape)
print("cada fila suma 1:", bool(np.allclose(A.sum(axis=1), 1.0)))
print("el producto y el bucle coinciden:", bool(np.allclose(E, E_bucle)))
print("pesos de a:  multiplicativa", d_h * d_h, " aditiva", 2 * d_a * d_h + d_a)
print("mayor peso de cada fila:", np.round(A.max(axis=1), 3), " uniforme:", round(1 / T_x, 3))
print()

# Wa = I: no hay cambio de base y la puntuacion es el producto escalar de los dos estados.
identidad = softmax_filas((S @ np.eye(d_h)) @ H.T)
print("con Wa = I, fila 1:", np.round(identidad[0], 3))
print("igual que puntuar con S @ H.T:", bool(np.allclose(identidad, softmax_filas(S @ H.T))))
`} />

La línea que importa es la del bucle: puntuar pareja a pareja da los mismos números que el producto,
que es lo único que hay que comprobar de la forma corta. La del mayor peso de cada fila trae una
sorpresa. Con los pesos recién
inicializados el mapa ya reparte de forma desigual —una fila se lleva $0.851$ cuando el reparto
uniforme sería $0.167$—, y eso no pasaba en la lección anterior con un tamaño parecido, donde el
mayor peso de cada fila rondaba el uniforme. La diferencia está en el $\tanh$: acota cada coordenada
antes de que $\mathbf{v}_a$ la lea, así que la puntuación aditiva nace pequeña. La multiplicativa no
tiene nada que la acote, y su tamaño crece con el de los estados. Ese pico no es una alineación, es
la dirección que $\mathbf{W}_a$ tomó al azar; lo que la fórmula garantiza no es empezar diciendo
algo, sino que haya por dónde entrenarlo. Que las puntuaciones sin acotar se disparen al crecer el
modelo es un problema de verdad, con un arreglo de una línea que llega en el bloque siguiente.

<PyCell packages={["numpy"]} code={`
import numpy as np
import time

T_y = T_x = 60
d_h, d_a = 256, 256
rng = np.random.default_rng(0)
S, H = rng.normal(size=(T_y, d_h)) * 0.5, rng.normal(size=(T_x, d_h)) * 0.5
Wa = rng.normal(size=(d_h, d_h)) * 0.05
Wsa, Wha = rng.normal(size=(d_a, d_h)) * 0.05, rng.normal(size=(d_a, d_h)) * 0.05
va = rng.normal(size=d_a)


def aditiva(S, H):
    P = (S @ Wsa.T)[:, None, :] + (H @ Wha.T)[None, :, :]   # (T_y, T_x, d_a): hay que construirlo
    return np.tanh(P) @ va                                  # (T_y, T_x)


def multiplicativa(S, H):
    return S @ (Wa @ H.T)                                   # (T_y, T_x): dos productos y nada mas


t0 = time.perf_counter(); Ea = aditiva(S, H)
t1 = time.perf_counter(); Em = multiplicativa(S, H)
t2 = time.perf_counter()

print("misma forma de salida:", Ea.shape, Em.shape)
print("productos y sumas:     aditiva %9d   multiplicativa %9d"
      % ((T_x + T_y) * d_a * d_h + T_x * T_y * d_a, T_x * d_h * d_h + T_x * T_y * d_h))
print("evaluaciones de tanh:  aditiva %9d   multiplicativa %9d" % (T_x * T_y * d_a, 0))
print("numeros intermedios:   aditiva %9d   multiplicativa %9d"
      % (T_x * T_y * d_a, T_x * d_h + T_x * T_y))
print("                       %.1f MB frente a %.2f MB (8 bytes por numero)"
      % (T_x * T_y * d_a * 8 / 1e6, (T_x * d_h + T_x * T_y) * 8 / 1e6))
print("tiempo:                aditiva %7.1f ms   multiplicativa %7.1f ms"
      % ((t1 - t0) * 1e3, (t2 - t1) * 1e3))
`} />

Las tres primeras líneas son la sección anterior contada por el código: la aritmética se parece, el
$\tanh$ y los $7.4$ MB intermedios no. La última depende de la máquina y del navegador, y aun así
dice algo, porque no está midiendo una diferencia pequeña: la multiplicativa tarda alrededor de un
orden de magnitud menos, y el factor exacto cambia de una ejecución a otra sin acercarse nunca a
uno. Y hay un experimento que lo deja claro. Cambia $d_a$ a $64$ y ejecuta otra vez: la aditiva pasa
a hacer **menos** aritmética que la multiplicativa —$2.2$ millones de operaciones contra $4.9$— y
sigue tardando varias veces más que ella. Lo que se está midiendo no es cuántas operaciones hay.

## Comprueba tu intuición

Cuatro preguntas: qué añade la matriz, cuánto pesa la puntuación, qué guarda la aditiva que la otra
no guarda, y si la rejilla se puede calcular de una vez.

<Quiz id="q-que-mide-wa" />

<Quiz id="q-parametros-luong" />

<Quiz id="q-memoria-intermedia" />

<Quiz id="q-sigue-secuencial" />

<CodeChallenge id="ch-atencion-multiplicativa" />

---

Con las dos puntuaciones delante hay algo que se ve mejor que con una sola. En las dos, el estado del
encoder entra en el mecanismo dos veces y por puertas distintas: una como argumento de $a$, que es
donde se decide cuánto peso se lleva, y otra como sumando de $\mathbf{c}_i$, que es donde su
contenido acaba llegando al decoder. El estado del decoder, en cambio, entra una sola vez, y siempre
del mismo lado: es el que pregunta. Tres papeles, y ninguna de las dos fórmulas exige que los dos
primeros los haga el mismo vector —lo hace porque nadie ha propuesto todavía separarlos.

Separarlos y ponerles nombre —consulta, clave y valor— es <Leccion slug="atencion-como-consulta">la lección siguiente, la última del bloque</Leccion>,
y es la más importante de este curso. No cambia ni una ecuación de las que ya has visto: reescribe
las mismas con las palabras y los símbolos con los que el bloque 5 lo escribe todo, incluida la
elección de puntuación que esta lección acaba de justificar. Lo que aquí quedó pendiente —que los
estados sigan llegando de uno en uno— tampoco lo arregla ese cambio de nombre, y por eso conviene
llegar al bloque siguiente con los nombres ya puestos.
